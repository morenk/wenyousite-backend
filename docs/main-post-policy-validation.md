# 主贴发言权限后端验收记录

本任务为既有聚合保存补充可选权限字段，业务语义见 [API 契约](api-contract.md#主贴发言权限的聚合保存)。不新增数据库迁移。负责人已在知悉隔离测试数据库缺口的情况下，明确授权合并并部署兼容后端；实际合并、部署与健康核验由治理任务及管理入口执行，不能将该授权记作真实数据库回归通过。

## 合入最新开发基线后的验证

- 任务分支合入开发基线 `2a2847a`，保留收藏夹可见计数、富文本块边界/行为语料和当前部署工具；没有改写共享历史。
- 从源 DTO 重新生成契约，候选版本见 [契约变更记录](../contracts/CHANGELOG.md)。与上一主贴权限候选相比，OpenAPI 仅版本及三处收藏夹计数说明变化；`SaveThreadAggregateDto` 与 `ThreadSubthreadResponseDto` 完全一致，旧主贴权限客户端请求和响应处理无需改变。
- 定向 DTO、聚合服务和 OpenAPI 回归 29 项通过；全量 Jest 165 个测试套件、2186 项测试通过；富文本离线测试 17 项通过。
- `pnpm check:full` 中的安全审计与完整 `pnpm check` 均通过，包含 lint、类型、架构、正式 OpenAPI 与版本检查、检查器并发隔离回归、全部 Jest/富文本/部署/安全脚本、文档及构建。生产依赖未发现已知漏洞。
- 初次交付中共享 `/tmp/wenyousite-openapi-check.json` 的 EACCES 问题已随基线的独占临时目录检查脚本修复；本次正式门禁直接通过，不再使用替代导出路径。

## 环境缺口与待补验

本次 `pnpm check:full` 在完整 `pnpm check` 和构建完成后，于 auth E2E 因工作区缺 `.env` 失败，最终退出码为 1；后续 economy、隔离 API、media/ranking E2E 未在该调用中执行。初次交付已单独执行这些入口，均因尚未配置测试数据库、缺 `.env` / `DATABASE_URL` 阻塞；本次不重复探查凭据、不使用生产数据回归、不另建基础设施。

`scripts/main-post-policy.e2e.ts` 已接入隔离 API 链且通过独立 TypeScript 检查，但尚未实际运行。具备授权的 loopback 可恢复测试环境后，应补跑 `pnpm check:full`，并验证：

- 游客不能管理；普通参与者、玩家和登录非成员不能修改权限；楼主与协作者可以保存。
- 三种权限下游客、楼主、协作者、玩家、普通参与者、登录非成员的详情能力，与楼层和楼中楼实际写入一致；公开帖登录非成员仅在开放策略首次发言后自动进入候选池。
- 标题、正文和权限联合保存，默认子贴版本只递增一次；其他子贴不变化。
- 旧客户端省略权限保留原值；非法值拒绝；默认子贴/正文版本冲突全部回滚。
- 后续停用标签校验失败，已执行的权限与正文修改真实回滚；并发同版本请求仅一个成功。

真实数据库事务、HTTP 权限矩阵与完整端到端检查仍未通过，不以模拟服务单元测试或发布授权代替。完整设置页与真机负责人验收仍待提供，保留任务 Worktree。

## 客户端与发布顺序

先合并并经管理入口部署兼容后端，再发布消费者；新客户端不能依赖旧部署后端接受新增请求字段。Web / Mobile 基于已提交 OpenAPI 与 Foundation 接入已有设置页统一保存，完整页面 UI 验收由各端提供，不能用后端检查代替。创建流程、其他子贴策略及现有接口均保留。
